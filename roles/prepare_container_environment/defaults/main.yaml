apt_packages:
  - podman
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg
  - lsb-release
  - uidmap
  - dbus-user-session
rootless_user: "{{ notdefined | mandatory(msg='This variable is required.') }}"