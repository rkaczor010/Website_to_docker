apt_packages:
  - podman
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg
  - lsb-release
  - uidmap
  - dbus-user-session
rootless_user: "{{ notdefined | mandatory(msg='This variable is required.') }}"
container_name: httpd
container_image: docker.io/library/httpd
container_ports_mappings:
  - "8080:80"